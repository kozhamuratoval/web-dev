let accessAllowed = (age > 18) ? true : false;
alert(accessAllowed);


let company = prompt("what company created JavaScript?", '');
let message = (company == 'Netscape') ? 'Right!' : 'Wrong.';
alert(message);